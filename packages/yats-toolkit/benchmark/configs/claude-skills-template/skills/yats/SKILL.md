---
name: yats
description: YATS has this codebase indexed in a knowledge graph. Use when the user asks any question about this codebase — how something works, architecture, call chains, routes, config, or where something is defined.
when_to_use: "How does X work?, Where is X defined?, What calls X?, architecture overview, trace the flow, find routes, find config, find tests, search code"
---

# YATS Code Intelligence
This project is indexed by YATS (mcp__yats__* tools). Every symbol, call, and relationship is in a knowledge graph with vector search.

## Golden rule
**YATS first, Read second.** mcp__yats__ tools return in milliseconds for ~100 tokens. Reading a file costs thousands of tokens.
